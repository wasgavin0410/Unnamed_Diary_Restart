apply plugin: 'com.android.application'

android {
    compileSdkVersion 4
    buildToolsVersion "27.0.3"

    defaultConfig {
        applicationId "com.mystictreegames.pagecurl"
        minSdkVersion 4
        targetSdkVersion 4
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
        }
    }
}
